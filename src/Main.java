import Controller.ToyStoreController;

public class Main {
    public static void main(String[] args) {
        ToyStoreController controller = new ToyStoreController();
        controller.start();
    }
}
